<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.hardware.camera2.full" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/book"
        android:label="Contact Book"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.natasaandzic.domaci1.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.natasaandzic.domaci1.activity.AddNewContactActivity"
            android:label="Add a new contact" />
        <activity
            android:name="com.natasaandzic.domaci1.activity.EditContactActivity"
            android:label="Edit contact" />
        <activity android:name="com.natasaandzic.domaci1.activity.ContactDetailsActivity" />
        <activity android:name="com.natasaandzic.domaci1.activity.CaptureActivity" />
    </application>

</manifest>